/**
 * Reexporta la matriz única de acceso por rol (`role-nav-access.ts`).
 * Importa desde aquí o desde `role-nav-access` indistintamente.
 */
export {
  SHELL_ROUTE_ROLES,
  SHELL_ROUTES,
  SIDEBAR_CATALOGOS_ADMIN_ROLES,
  SIDEBAR_PROVEEDORES_NAV_ROLES,
  roleCanAccessRoute,
  rolesForShellRoute,
  type ShellNavRoutePath
} from './role-nav-access';
